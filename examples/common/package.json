{
  "name": "@warden-community-agents/common",
  "version": "1.0.0",
  "description": "Common utilities and types for community agents.",
  "private": true,
  "type": "module",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "clean": "rimraf build",
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -w -p tsconfig.json",
    "lint": "eslint src"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
