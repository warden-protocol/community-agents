{
  "name": "@warden-community-agents/portfolio-agent",
  "version": "1.0.0",
  "description": "Portfolio agent for cryptocurrency wallet performance analysis.",
  "private": true,
  "type": "module",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "start": "tsx src/index.ts",
    "clean": "rimraf build",
    "prebuild": "npm run lint",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run unit --config tests/vitest.config.ts",
    "test:coverage": "vitest run unit --config tests/vitest.config.ts --coverage.enabled --coverage.all"
  },
  "dependencies": {
    "@warden-community-agents/common": "^1.0.0",
    "@langchain/core": "^0.3.78",
    "@langchain/langgraph": "^0.4.9",
    "@langchain/openai": "^0.6.16",
    "@langchain/anthropic": "^0.3.31",
    "@langchain/mcp-adapters": "^0.6.0",
    "@coingecko/coingecko-mcp": "^2.2.0",
    "dotenv": "^16.4.7",
    "langchain": "^0.3.36",
    "zod": "^3.24.1"
  }
}
